<%= form_for([@run.book,@run]) do |f| %>
  <% if @run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@run.errors.count, "error") %> prohibited this run from being saved:</h2>

      <ul>
      <% @run.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="hidden">
    <%= f.hidden_field :book_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
